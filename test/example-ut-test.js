
const assert = require('chai').assert;

/**
 * Unit Tests
 */
describe('Example', () => {
  describe('Test', () => {
    it('should return ...', async () => {
      assert.equal(0, 0);
    });
  });
});
